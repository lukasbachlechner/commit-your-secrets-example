{
  "name": "commit-your-secrets-example",
  "private": true,
  "type": "module",
  "scripts": {
    "tools": "brew install sops age",
    "keygen": "mkdir -p ~/.config/sops/age && { [ -f ~/.config/sops/age/keys.txt ] || age-keygen -o ~/.config/sops/age/keys.txt; } && echo 'your public key — paste it into .sops.yaml:' && age-keygen -y ~/.config/sops/age/keys.txt",
    "dev": "./scripts/with-env.sh node app.mjs",
    "secrets": "SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-keys/demo-key.txt} sops secrets/dev.yaml",
    "grant": "SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-keys/demo-key.txt} sops updatekeys secrets/dev.yaml"
  }
}
